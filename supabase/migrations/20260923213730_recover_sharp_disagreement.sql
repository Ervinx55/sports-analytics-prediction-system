-- Reconstructed from production catalog on 2026-09-23.\n-- Catch-up source for fresh environments; do not replay blindly on current production.\n\ncreate table if not exists public.sharp_disagreement_shadow (
  sharp_gate_id bigint not null,
  evaluated_at timestamp with time zone default now() not null,
  sport text not null,
  event_id text not null,
  starts_at timestamp with time zone,
  market_type text not null,
  market_side text not null,
  market_line numeric,
  classification text not null,
  confidence numeric not null,
  reason_code text not null,
  reason text not null,
  valid_source_count integer default 0 not null,
  fresh_source_count integer default 0 not null,
  stale_source_count integer default 0 not null,
  invalid_source_count integer default 0 not null,
  unavailable_source_count integer default 0 not null,
  spread_pp numeric,
  sharp_data_quality numeric,
  source_age_gap_minutes numeric,
  outlier_source text,
  outlier_distance_pp numeric,
  movement_direction text,
  movement_source_count integer default 0 not null,
  movement_magnitude numeric,
  movement_window_minutes integer default 60 not null,
  source_diagnostics jsonb default '{}'::jsonb not null,
  movement_diagnostics jsonb default '{}'::jsonb not null,
  shadow_only boolean default true not null,
  affects_decision boolean default false not null,
  constraint sharp_disagreement_shadow_classification_check CHECK (classification = ANY (ARRAY['CONSENSUS_OK'::text, 'STALE_PRICE'::text, 'MARKET_MOVING'::text, 'REAL_SHARP_DISAGREEMENT'::text, 'SOURCE_QUALITY_PROBLEM'::text, 'INSUFFICIENT_SOURCES'::text, 'DISAGREEMENT_WATCH'::text])),
  constraint sharp_disagreement_shadow_confidence_check CHECK (confidence >= 0::numeric AND confidence <= 1::numeric),
  constraint sharp_disagreement_shadow_sharp_gate_id_fkey FOREIGN KEY (sharp_gate_id) REFERENCES sharp_gate_history(id) ON DELETE CASCADE,
  constraint sharp_disagreement_shadow_pkey PRIMARY KEY (sharp_gate_id)
);\nalter table public.sharp_disagreement_shadow enable row level security;\nrevoke all on table public.sharp_disagreement_shadow from public, anon, authenticated;\ngrant select, insert, update, delete on table public.sharp_disagreement_shadow to service_role;\n\nCREATE INDEX IF NOT EXISTS sharp_disagreement_classification_idx ON public.sharp_disagreement_shadow USING btree (classification, evaluated_at DESC);\n\nCREATE INDEX IF NOT EXISTS sharp_disagreement_market_idx ON public.sharp_disagreement_shadow USING btree (sport, event_id, market_type, market_side, market_line, evaluated_at DESC);\n\ncreate or replace view public.sharp_disagreement_latest with (security_invoker = true) as\nSELECT d.sharp_gate_id,
    d.evaluated_at,
    d.sport,
    d.event_id,
    d.starts_at,
    d.market_type,
    d.market_side,
    d.market_line,
    d.classification,
    d.confidence,
    d.reason_code,
    d.reason,
    d.valid_source_count,
    d.fresh_source_count,
    d.stale_source_count,
    d.invalid_source_count,
    d.unavailable_source_count,
    d.spread_pp,
    d.sharp_data_quality,
    d.source_age_gap_minutes,
    d.outlier_source,
    d.outlier_distance_pp,
    d.movement_direction,
    d.movement_source_count,
    d.movement_magnitude,
    d.movement_window_minutes,
    d.source_diagnostics,
    d.movement_diagnostics,
    d.shadow_only,
    d.affects_decision
   FROM sharp_disagreement_shadow d
     JOIN sharp_gate_latest g ON g.id = d.sharp_gate_id;\n;\nrevoke all on table public.sharp_disagreement_latest from public, anon, authenticated;\ngrant select on table public.sharp_disagreement_latest to service_role;\n